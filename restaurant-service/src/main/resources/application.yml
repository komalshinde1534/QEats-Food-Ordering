spring:
  application:
    name: restaurant-service
  datasource:
    url: jdbc:mysql://localhost:3306/qeats_restaurant?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
    username: qeats
    password: qeats
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
server:
  port: 8081
