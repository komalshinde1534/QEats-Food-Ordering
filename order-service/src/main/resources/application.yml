spring:
  application:
    name: order-service
  datasource:
    url: jdbc:mysql://localhost:3306/qeats_order?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
    username: qeats
    password: qeats
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
  data:
    redis:
      host: localhost
      port: 6379
server:
  port: 8082
